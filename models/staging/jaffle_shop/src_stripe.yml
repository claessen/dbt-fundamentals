version: 2

sources:
    - name: stripe
      database: raw
      schema: stripe
      tables:
        - name: payment
          freshness:
            warn_after: 
                count: 10
                period: hour
            error_after: 
                count: 20
                period: hour
          loaded_at_field: _BATCHED_AT